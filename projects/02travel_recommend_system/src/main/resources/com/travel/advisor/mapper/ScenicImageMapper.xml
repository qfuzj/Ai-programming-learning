<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.travel.advisor.mapper.ScenicImageMapper">


    <insert id="insertBatch" parameterType="list">
        INSERT INTO scenic_image (scenic_spot_id, file_resource_id, image_url, image_type, title, sort_order, is_cover)
        VALUES
            <foreach collection="list" item="item" separator=",">
                (#{item.scenicSpotId}, #{item.fileResourceId}, #{item.imageUrl}, #{item.imageType}, #{item.title}, #{item.sortOrder}, #{item.isCover})
            </foreach>

    </insert>
</mapper>