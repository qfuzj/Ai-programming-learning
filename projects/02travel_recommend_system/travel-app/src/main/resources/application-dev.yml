spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/travel_recommend_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: wo221000
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      password:
      database: 0
      timeout: ${REDIS_TIMEOUT:500ms}
      jedis:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5

logging:
  level:
    com.travel.advisor: debug
    org.springframework.data.redis: debug
